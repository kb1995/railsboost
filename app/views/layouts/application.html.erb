<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>

  <body class="min-h-screen flex flex-col">
    <div class="flex-grow">
      <%= render 'shared/navbar' %>
      <%= render 'shared/notices' %>

      <main>
        <%= yield %>
      </main>
    </div>

    <%= render 'shared/footer' %>
  </body>
</html>
